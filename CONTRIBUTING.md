# Contributing

Thanks for improving `runledger-skill`.

## Local Setup

```bash
npm install
npm run build
npm test
```

## Pull Requests

- Keep changes local-first and deterministic.
- Add fixtures for new ledger behavior.
- Update `SKILL.md` when agent workflow boundaries change.
- Run `bash scripts/validate.sh` before opening a PR.

